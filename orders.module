<?php

/**
 * Implements hook_menu().
 */
function orders_menu() {
 
  $items['orders/example'] = array(
    'page callback' => '_print_order',
    'page arguments' => array(2),
    'access callback' => TRUE,
    'type' => MENU_CALLBACK,
  );
  
  return $items;
}


function _print_order(){
	
 echo 'first function';

}
